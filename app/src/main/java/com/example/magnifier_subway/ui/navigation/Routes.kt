package com.example.magnifier_subway.ui.navigation

import com.example.magnifier_subway.Screen

// 앱이라는 큰도시의 도로명 주소.
// object로 묶어서 오타 방지 및 지도 카타로그 역할. 유지보수 높아짐
object Routes {
    const val SPLASH = "splash"
    const val LOGIN = "login"
    const val MAIN = "main" // 나중에 메인화면 붙일때

    const val  SEARCH = "search" // 역이름
    const val  RESULT = "result" // 결과물
}